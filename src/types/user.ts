export type User = {
    id?: string
    name: string
    lastname: string
    email: string
    username: string
    password?: string
    profileImage?: string
    access_token: string
    createdAt: string,
    updatedAt: string
}